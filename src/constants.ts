export enum EngineEnum {
  mysql = 'mysql',
  postgres = 'postgres',
  mssql = 'mssql',
  sqlite = 'sqlite',
}
export enum FrameworkEnum {
  nestjs = 'nestjs',
  expressjs = 'expressjs',
}

export enum PatternEnum {
  ddd = 'ddd',
  repository = 'repository',
}
